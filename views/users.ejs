<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>AuthX</title>
</head>

<body>

    <div class="w-full h-screen bg-[black] text-white px-10 py-20">
        <h1 class="text-2xl my-4">All Users</h1>
        <div class="flex flex-col w-fit gap-4">

            <ul class="flex flex-wrap gap-4">

                <% allusers.forEach(user=>{ %>

                    <li>
                        <div class="flex flex-col border px-2 py-2 rounded-md"><span>name : <%= user.name %></span>
                            <span>Email : <%= user.email %></span>
                            <div class="flex gap-4 "><a href="/edit/<%= user._id %>"
                                    class="text-zinc-400 cursor-pointer ">Edit User</a>
                                <a href="/delete/<%= user._id %>" class="text-red-500 cursor-pointer">Delete User</a>
                            </div>
                        </div>

                    </li>

                    <% }) %>



            </ul>

        </div>
    </div>

</body>

</html>